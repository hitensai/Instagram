%h1= @pic.title
%p= @pic.description
%p
Pick by
= @pic.user.email

%br
%hr

= link_to "Back", root_path
= link_to "Edit", edit_pic_path
= link_to "delete", pic_path(@pic), method: :delete, data: {confirm: "Are you sure?"},class: "delete" 
