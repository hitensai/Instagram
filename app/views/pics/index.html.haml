.col-md-5.col-md-offset-3
  #pics.transitions-enabled
    - @pics.each do |pic|
      .box.panel.panel-default
        = link_to (image_tag pic.image.url, :size => '1100x200'), pic
        %h2= link_to pic.title, pic

  
  